# Output from the local all-torch build helper.
*_build.log

# Local build configuration; it may contain machine-specific paths or credentials.
.env

# Project-local build interpreter and retained wheel artifacts.
.venv/
dist/
__pycache__/

# Local wheel and build outputs.
.build/
